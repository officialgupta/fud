%div.number  
  %h1= "There are #{@foodbanks.count} foodbanks within #{@distance} miles of your #{@home ? 'home' : 'location'}"

%div.banks
  %div.location  
    %ul
      - @foodbanks.each do |foodbank|
        %li
          %h3
            = link_to foodbank_path(id: foodbank.id, loc: @loc) do
              = "#{foodbank.name}, #{foodbank.county}"
              %small= foodbank.company
            %label
              = foodbank.distance_to(@loc).round(1)
              miles from your
              = @home ? 'home' : 'location'

I am donating
%ul
  - current_user.items.donating.each do |item|
    %li= item.food.name

= link_to "Click here when you have donated these items!", donated_items_path
%br
%small= link_to "Not at home? Click here to find your location by GPS.", "#", id: "gps-link"

%br
%br
%br
%br
